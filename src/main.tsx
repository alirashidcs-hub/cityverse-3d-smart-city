import React from "react";
import ReactDOM from "react-dom/client";
import CityVerse from "./CityVerse";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CityVerse />
  </React.StrictMode>
);
